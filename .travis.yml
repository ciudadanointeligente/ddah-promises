language: python
python:
  - "2.7"
install:
  - "pip install -r tests_requirements.txt"
  - "python setup.py develop"
  - "pip install coveralls"
script:
  - "./manage.py test"
  - "coverage run --source=promises manage.py test"
after_success:
  - coverage report -m
  - coveralls
